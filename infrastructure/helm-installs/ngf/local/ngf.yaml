apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ngf
  namespace: nginx-gateway
spec:
  interval: 30m
  chart:
    spec:
      chart: ngf
      version: "*"
      sourceRef:
        kind: OCIRepository
        name: ngf
        namespace: flux-system
      interval: 12h