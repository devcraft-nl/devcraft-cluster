apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: grafana-stack

configMapGenerator:
  - name: alloy-config
    files:
      - config.alloy
generatorOptions:
  disableNameSuffixHash: true

resources:
  - ../base
  - loki-credentials.yaml